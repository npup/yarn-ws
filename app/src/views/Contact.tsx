import { Button } from "lib-components/src/Button";
import * as React from "react";

export function Contact() {
    return (
        <div>
            contact <Button signal="no">En knapp</Button>
        </div>
    );
}
